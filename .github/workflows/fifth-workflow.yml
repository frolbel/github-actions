name: fifth-workflow
on: 
  - push
  - issues
  - pull_request
jobs:
  run-fiveth-commamnd:
    runs-on: ubuntu-latest
    steps:
      - name: Event Vars
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF